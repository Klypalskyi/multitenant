{
  "name": "@multitenant/cli",
  "version": "0.5.1",
  "bin": {
    "multitenant": "./dist/cli.js"
  },
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/cli.ts src/index.ts --format cjs,esm --dts",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.5",
    "vitest": "^3.0.5"
  },
  "dependencies": {
    "@multitenant/core": "^0.4.0",
    "@multitenant/config": "^0.4.0",
    "@multitenant/dev-proxy": "^0.4.0",
    "chokidar": "^3.6.0",
    "commander": "^12.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
