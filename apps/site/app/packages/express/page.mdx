# `@multitenant/express`

**Express** middleware: **`req.tenant`** (and related fields) after **`createTenantRegistry`** resolves the incoming **Host** / forwarded headers for the active **environment**.

## Install

```bash
npm install @multitenant/express express
```

Peer: **`express`**.

## Main API

- **`multitenantExpress({ registry, environment })`** — returns Express middleware

## See also

- [Express](/frameworks/express) — full example and production notes
- [`@multitenant/core`](/packages/core) — **`createTenantRegistry`**
